<% @tasks.each do |t| %>
  <h2><%= link_to t.title, task_path(t) %></h2>
  <p><%= t.description %></p>
  <%= link_to 'Edit Post', edit_task_path(t) %>
  <%= link_to 'Delete Post', task_path(t), method: :delete %>
<% end %>

<p><%= link_to 'Add new task', new_task_path %></p>
